# 技术文档 · CacheCleaner 智能缓存清理工具

> 本文档由 WorkBuddy 静态扫描源码生成，记录脚本的技术架构、核心模块与修改历史。
> 配套说明文档见 `README.md`。

---

## 一、项目概述

| 项 | 内容 |
|----|------|
| 用途 | 自动扫描用户目录下的 AI 缓存、浏览器/软件/系统缓存、**系统软件升级缓存**、临时文件与**注册表垃圾**，支持**通用结构识别**、风险分级、交互选择、安全清理 |
| 目标平台 | **Windows / macOS / Linux**（三平台静态二进制）；注册表清理仅 Windows |
| 运行时 | Go 编译产物无运行时依赖；GUI 版使用 **Wails v2**（内嵌 WebView2/系统 WebView），CLI 版纯标准库（+ Windows 下 `golang.org/x/sys`） |
| Go 版本 | **Go 1.25**（`go.mod` 声明 1.25.0） |
| 当前版本 | **v3.3**（Fluent 2 界面重构；v3.2 的注册表清理 + 升级缓存识别 + 豆包误删修复 + 并发整改） |
| 入口 | GUI：`main.go`（Wails 绑定 `app.go`，前端 `frontend/dist` 原生三件套）；CLI：`cmd/cli/main.go`（`go run ./cmd/cli`） |
| 界面 | **Windows 11 Fluent 2**：Mica 桌面底纹 + Acrylic 毛玻璃（`backdrop-filter`）+ 1px 描边；浅色/深色双主题（跟随系统 + 手动切换 + localStorage 记忆）；响应式三档（≥1081px 完整侧栏 / ≤1080px 图标栏 / ≤720px 底部应用栏） |
| 代码规模 | `main.go` + `app.go` + `internal/{model,config,db,scan,ui,clean,regclean}` + `cmd/cli`，约 1900 行（不含测试） |
| 校验 | `go build ./...`、`go vet ./...`、`go test ./...` 全部通过（含注册表白名单、排除匹配、去重、清理历史、App 并发用例） |

---

> **版本说明**：v3.0 起核心实现由 PowerShell 脚本重构为 **Go 跨平台程序**（仓库根目录 `main.go` + `internal/`）。
> 旧版 `CacheCleaner.ps1` / `CacheCleaner.bat` 仍保留作参考，但**已停止维护**。下文「PowerShell 版架构」仅存档旧实现细节。

### v3.x 跨平台架构（Go）

```
main.go                 Wails GUI 入口（Bind: App）
  ├─ app.go            前端绑定层：Scan / CleanSelected / 自定义目录 / 历史 / 注册表
  │    └─ 进度经事件 scan:progress → scan:done 回传，扫描在 goroutine 中执行
  │
  ├─ 文件缓存扫描（scan 引擎，四阶段：已知 1 / 自动发现 2 / 通用识别 3 / 自定义 4）
  │    ├─ db.BuildKnownCaches()      三系统已知缓存库（含升级缓存，按 OS 适配）
  │    ├─ scan.AutoDiscover()        关键词自动发现（npm/pip/maven… + ProgramData）
  │    ├─ scan.FindElectronCaches()  通用结构识别（不认工具名）
  │    │      └─ db.ElectronSafeDirs / ElectronExcludeDirs
  │    └─ scan.FindCustomCaches()    用户自定义目录
  │              ▼
  │       ui.SelectEntries() → clean.Clean()（风险分级 + 排除 + 历史批量原子写）
  │
  └─ 注册表清理（regclean，仅 Windows；其他平台为空实现）
       ├─ regclean.Scan()   扫描 HKCU 下已知 MRU/缓存位置（白名单表）
       └─ regclean.Clean()  只删值不删键 + 白名单校验 → clean.AppendHistory()

cmd/cli/main.go          命令行入口（同一套 internal 包）
  └─ ui.ShowMainMenu()  ── [1] AI 扫描 [2] 一键扫描 [3] 目录管理 [4] 历史 [5] 注册表清理
```

**关键技术点**
- **OS 自适应路径**：`config` 包用 `os.UserHomeDir/UserCacheDir/UserConfigDir` 自动映射到
  Windows `%LocalAppData%/%AppData%`、macOS `~/Library/Caches`、`~/Library/Application Support`、
  Linux `~/.cache`、`~/.config`，无需硬编码。
- **通用结构识别（核心卖点）**：`FindElectronCaches` 遍历 `AppData` 三目录 + 用户根隐藏目录，
  命中 `Cache/GPUCache/Code Cache/Dawn*Cache/blob_storage/Crashpad/CachedData` 等标准缓存名即判定为
  可清理缓存簇，并自动排除 `Cookies/Local State/IndexedDB/Preferences` 等登录态文件。
  **任何新出的 Electron 套壳 AI 工具装了即被自动识别**，无需维护清单。
- **零依赖静态编译**：CLI 纯 Go 标准库（注册表清理在 Windows 下用到 `golang.org/x/sys/windows/registry`）；
  GitHub Actions 矩阵（`windows/amd64`、`linux/amd64`、`darwin/amd64`、`darwin/arm64`）一键出包并自动发 Release。
- **系统软件升级缓存识别（v3.2）**：新增 `BaseProgramData` / `BaseWindows` 两个路径锚点，
  覆盖 `Tencent\xwechat\update`（微信 4.x，**本机实测 1.3GB**）、企业微信/QQ邮箱升级缓存、
  `SquirrelTemp`、`C:\ProgramData\Package Cache`（**本机 191MB**）、NVIDIA 下载缓存、
  Windows 更新下载缓存与传递优化缓存、`C:\Windows\Temp`；`ProgramData` 同时纳入自动发现与通用识别的扫描根。
- **注册表垃圾清理（v3.2）**：`internal/regclean` 包，采用与文件缓存一致的"已知位置 + 风险分级"策略，
  扫描 HKCU 下的 RunMRU / RecentDocs / TypedPaths / ComDlg32 MRU / MuiCache / TrayNotify 等位置；
  **只删值不删键**，清理前做白名单校验（非白名单键一律拒绝），Windows 会在需要时自动重建。
- **排除规则统一（v3.2）**：`config.Config.IsExcluded()` 为扫描与清理的唯一判定入口，采用
  "完整路径段 + 大小写不敏感"匹配，避免旧实现中 `strings.Contains` 的子串误伤（排除 `logs` 会误伤 `Catalogs`）。

### 前端界面架构（v3.3，Windows 11 Fluent 2）

```
frontend/dist/
  ├─ index.html   语义化结构 + 内联 SVG 图标精灵（<symbol>，24×24 / 1.6 描边 / 圆角端点）
  ├─ style.css    设计令牌 → 材质 → 组件 → 响应式三档 → 降低动效偏好
  └─ app.js       编排层：主题 / 扫描 / 清理 / 目录规则 / 注册表 / 历史 / 弹窗 / Toast
```

- **设计令牌**：全部颜色、圆角（控件 6px / 卡片 12px）、间距、动效曲线集中在 `:root` 与
  `[data-theme="dark"]`，主题切换只改 `documentElement.dataset.theme`。
- **材质**：`.mica` 固定层用三层柔和光晕 + `feTurbulence` 噪点模拟 Win11 桌面底纹；
  `.acrylic` 用 `backdrop-filter: blur(38px) saturate(165%)` + 半透明底色 + 1px 描边，
  顶栏 / 侧栏 / 卡片 / 对话框 / Toast 共享同一材质变量。
- **动效**：Fluent 缓动曲线 `cubic-bezier(.33,0,.67,1)`；面板错峰揭示（`--d` 自定义属性控制延迟）、
  行与图表条入场、勾选框打勾动画、对话框缩放淡入、进度条确定/不确定双态；
  `prefers-reduced-motion` 下全部降级。
- **交互细节**：Win11 风格焦点环、Esc 与点击遮罩关闭弹窗、打开弹窗时移焦并在关闭后归还焦点、
  扫描期间禁用按钮、清理失败项保留在列表中、所有文本渲染前 `esc()` 转义。
- **主题首屏防闪**：`index.html` 头部内联脚本在首绘前确定主题。

---

## 二、文件清单

| 文件 | 类型 | 作用 | 备注 |
|------|------|------|------|
| `CacheCleaner.bat` | 批处理 | 启动入口 | 关闭回显 → 切到脚本目录 → 调用 ps1 → `pause` |
| `CacheCleaner.ps1` | PowerShell | 主程序（全部逻辑） | 单文件脚本，无外部依赖 |
| `README.md` | 文档 | 用户使用说明 | 功能、用法、安全须知 |
| `config/CacheCleaner.config.json` | 配置 | 用户自定义目录/排除项（运行时生成） | 已在 `.gitignore` 忽略 |
| `config/clean_history.json` | 数据 | 清理历史记录（运行时生成） | 已在 `.gitignore` 忽略 |

---

## 三、整体架构

脚本采用**单文件、配置驱动、分层函数**结构，逻辑自上而下分为六层：

```
┌─ 入口层 ──────────────────────────────┐
│  CacheCleaner.bat  (cmd 启动)           │
└──────────────┬─────────────────────────┘
               ▼
┌─ 初始化层 ────────────────────────────┐
│  窗口尺寸设置(mode/RawUI/Win32 API)     │
│  管理员权限检测                          │
└──────────────┬─────────────────────────┘
               ▼
┌─ 配置层 ──────────────────────────────┐
│  $Script:Config   (扫描范围/关键词/阈值) │
│  $Script:KnownCaches (60+ 已知缓存路径) │
└──────────────┬─────────────────────────┘
               ▼
┌─ 扫描/发现层 ─────────────────────────┐
│  Find-KnownCaches   (精确匹配已知库)    │
│  Auto-DiscoverCaches (关键词+特征发现)  │
│  Find-LargeFiles     (>50MB 大文件)     │
└──────────────┬─────────────────────────┘
               ▼
┌─ 交互/UI 层 ──────────────────────────┐
│  Show-MainMenu / Select-CleanItems      │
│  Show-CacheList / Write-Color 等        │
└──────────────┬─────────────────────────┘
               ▼
┌─ 执行/持久层 ─────────────────────────┐
│  Invoke-CleanCache  (删除+占用检测)      │
│  Save/Load-CustomConfig / clean_history │
└───────────────────────────────────────┘
```

核心数据流：**扫描 → 合并去重 → 风险分级排序 → 交互选择 → 二次确认 → 清理 → 写入历史**。

---

## 四、核心模块与函数

### 4.1 入口与初始化
- **`CacheCleaner.bat`**：`cd /d "%~dp0"` 保证从脚本目录运行；`powershell -NoProfile -ExecutionPolicy Bypass -File` 绕过执行策略限制；末尾 `pause` 防止窗口闪退。
- **窗口尺寸设置（行 ~1236–1294）**：三套方案叠加兼容不同宿主——`mode.com`（`con: cols/lines`）、PowerShell `RawUI` 缓冲/窗口尺寸、Win32 API（`GetConsoleWindow`/`MoveWindow`）精确调整像素尺寸并居中。
- **管理员检测（行 ~1297）**：`WindowsPrincipal.IsInRole(Administrator)`，非管理员仅提示不阻断。

### 4.2 配置与已知缓存库
- **`$Script:Config`（行 14–52）**：集中配置——扫描根目录（`$env:USERPROFILE`）、排除目录名、缓存关键词（中英文大小写变体）、缓存扩展名、大文件阈值（50MB）、旧文件阈值（30 天）。
- **`$Script:KnownCaches`（行 58–134）**：60+ 条内置缓存路径，按 `Category`（AI缓存/浏览器缓存/开发工具缓存/IDE缓存/系统临时文件/软件缓存/需谨慎）与 `Risk`（Safe/Caution/Review）分类，覆盖 Cursor、Trae、VS Code、Copilot、HuggingFace、Ollama、Chrome、Edge、npm、pip、Gradle、JetBrains 等。

### 4.3 辅助与 UI 函数
| 函数 | 作用 |
|------|------|
| `Format-FileSize` | 字节→TB/GB/MB/KB 人类可读 |
| `Get-DirectorySize` / `Get-DirectoryFileCount` / `Get-DirectoryLastAccess` | 目录元信息收集（带异常兜底，返回 0） |
| `Write-Color` / `Write-Header` / `Write-Separator` | 彩色/自适应宽度控制台输出 |
| `Get-ConsoleWidth` / `Center-Text` / `Get-DisplayWidth` / `Repeat-Char` | **控制台宽度自适应，正确处理中文字符占 2 列** |

### 4.4 扫描与发现
- **`Find-KnownCaches`（行 259）**：遍历 `KnownCaches`，支持通配符路径（如 `JetBrains\*\caches`），计算大小/文件数/最后访问时间。
- **`Auto-DiscoverCaches`（行 321）**：在 `AppData\Local|Roaming|LocalLow` 下按深度 ≤3 递归；跳过 `ExcludeDirs` 与已知目录；**双重识别**——目录名匹配 `CacheKeywords` **或** 缓存类扩展名占比 >50%（且文件数 >10）即判定为缓存；按大小降序取前 50。
- **`Find-LargeFiles`（行 433）**：在 Temp / INetCache 下扫描 ≥50MB 文件，取前 20。

### 4.5 清理执行
- **`Invoke-CleanCache`（行 469）**：逐项处理——不存在则跳过；文件先尝试 `File.Open` 检测占用，被占用则跳过；目录采用"清空内容、保留目录本身"策略；统计释放空间、成功/失败计数；失败项打印异常信息不中断。

### 4.6 交互与菜单
| 函数 | 作用 |
|------|------|
| `Show-MainMenu`（行 828） | 主菜单循环，分发 0–9 / q 到各 Invoke 函数 |
| `Invoke-SmartScan`（行 910） | 已知+自动发现合并去重，按分类筛选，交互选择并二次确认，写入历史 |
| `Invoke-DeepAIClean`（行 996） | 重点功能：AI 缓存扫描 + 深度特征文件扫描（`*.aicache`/`*.completions`/`*.copilot` 等 12 种模式），按 AI 子分类统计 |
| `Invoke-QuickClean`（行 1139） | 仅清理 `Risk=Safe` 且大小 >0 的项 |
| `Invoke-LargeFileScan`（行 1170） | 大文件列出（清理归入其他功能） |
| `Select-CleanItems`（行 644） | 解析 `1,3,5-8` / `all` / `safe` / `q` 输入 |
| `Manage-CustomDirs`（行 713） | 增删自定义目录、排除目录，扫描并清理 |
| `Show-CleanHistory`（行 1199） | 读取 `clean_history.json` 累计统计 |

---

## 五、核心数据结构

### 缓存项对象（扫描结果统一结构）
```powershell
[PSCustomObject]@{
    Path       = "完整路径"
    ShortPath  = "相对用户目录的短路径"
    Category   = "AI缓存 / 浏览器缓存 / ..."
    Risk       = "Safe | Caution | Review"
    Desc       = "人类可读描述"
    Size       = <long 字节数>
    SizeStr    = "格式化大小"
    FileCount  = <int>
    LastAccess = <DateTime>
}
```
> 大文件项额外含 `DaysOld` 字段（用于区分文件/目录与选择逻辑）。

### 历史记录（`clean_history.json`）
```json
{ "Time": "yyyy-MM-dd HH:mm:ss", "Items": 12, "Freed": 123456789, "FreedStr": "117.74 MB", "Category": "AI深度清理" }
```

---

## 六、风险分级模型

| 等级 | 颜色 | 含义 | 典型示例 |
|------|------|------|----------|
| `Safe` | 绿 | 可放心清理，不影响功能 | 浏览器缓存、npm 缓存、Temp |
| `Caution` | 黄 | 清理后需重新加载/重建/登录 | VS Code workspaceStorage、JetBrains 缓存、Teams 缓存 |
| `Review` | 红 | 需用户确认，可能耗费重新下载成本 | Ollama/LM Studio 本地模型、OneDrive 设置 |

排序规则：Safe(0) → Caution(1) → Review(2)，同级按大小降序，确保低风险大体积项优先展示。

---

## 七、关键技术点

1. **版本锁定**：`#Requires -Version 5.1`，兼容 Win10/11 自带 PowerShell，无需额外安装。
2. **零外部依赖**：纯 PowerShell 标准库（`System.IO`、`System.Management.Automation.Host`、`kernel32/user32` P/Invoke），无第三方模块。
3. **控制台自适应**：`Get-ConsoleWidth` 限制 60–120 列；`Get-DisplayWidth` 按 unicode >127 计 2 列，解决中文对齐错乱。
4. **窗口尺寸三方案叠加**：`mode.com` + `RawUI` + Win32 `MoveWindow`，覆盖 cmd / powershell / Windows Terminal 不同宿主。
5. **智能发现双策略**：关键词匹配 + 缓存扩展名占比分析，降低漏报。
6. **安全删除**：删除前 `File.Open` 探测占用；目录删除保留父目录；全程异常捕获不中断循环。
7. **配置环境变量展开**：`[Environment]::ExpandEnvironmentVariables` 支持 `%USERPROFILE%` 等写法。
8. **历史持久化**：每次成功清理追加写入 `clean_history.json`，支持累计统计。

---

## 八、安全机制

- **交互确认**：每一项清理前列出路径/大小/风险，用户编号选择；选择后再次 `y` 确认（深度/分类清理）。
- **风险分级**：红/黄/绿三级可视化，高风险项强制人工确认。
- **排除目录**：`ExcludeDirs` 默认屏蔽 `Windows`、`Program Files`、`node_modules` 等系统/工程目录，避免误删。
- **占用检测**：文件被进程锁定则跳过，避免破坏正在运行的程序。
- **仅删缓存**：目录清理仅删除内容、保留了目录结构；不触碰用户文档。
- **配置隔离**：`config/` 含用户路径信息，已加入 `.gitignore`。

---

## 九、运行流程（主路径）

```
bat 启动
  → 初始化窗口 / 管理员提示
  → Show-MainMenu 循环
      ├─ [1] Invoke-DeepAIClean    深度 AI 缓存
      ├─ [2] Invoke-SmartScan all  全量扫描
      ├─ [3-6] Invoke-SmartScan 分类
      ├─ [7] Invoke-QuickClean     仅 Safe
      ├─ [8] Manage-CustomDirs     自定义目录
      ├─ [9] Invoke-LargeFileScan  大文件
      └─ [0] Show-CleanHistory     历史
            ↓ 选择项 → 确认(y) → Invoke-CleanCache → 写 clean_history.json
```

---

## 十、自定义配置

编辑 `config/CacheCleaner.config.json`（首次运行/自定义时自动创建）：

```json
{
    "CustomDirs": [ "%USERPROFILE%\\AppData\\Local\\CustomCache" ],
    "ExcludedDirs": [ "node_modules", ".git" ]
}
```

也可通过菜单 **[8] 管理自定义目录** 交互增删。

---

## 十一、修改记录（CHANGELOG）

| 版本 | 日期 | 修改内容 | 来源 |
|------|------|----------|------|
| v2.0 | 此前版本 | 初始完整实现：配置驱动扫描、60+ 已知缓存库、智能自动发现、风险三级分级、交互选择清理、AI 深度扫描、自定义目录管理、清理历史记录、控制台自适应与窗口尺寸调整 | 源码横幅/逻辑推断 |
| — | 2026-08-15 | **新增 `技术文档.md`**：梳理架构、核心模块函数、数据结构、风险模型、关键技术点、安全机制与运行流程；建立本文档的修改记录表 | WorkBuddy 静态扫描 |
| v2.1 | 2026-08-15 | **AI 缓存库重大扩充 + 修复 2 处 bug**：① 修正 Trae 错误路径(`AppData\Local\Trae`→真实 `AppData\Roaming\Trae CN`+`.trae-cn`)；② 新增 WorkBuddy/CodeBuddy、通义灵码/Qoder、Claude、Windsurf/Codeium、文心快码、Cline/Continue、豆包等国产/主流 Agent 工具真实缓存条目(路径经本机实测/官方文档核对)；③ 修复 `Find-KnownCaches` 通配符硬编码 JetBrains 导致 `.vscode\extensions\*` 等失效，改为通用 `Resolve-Path` 展开；④ 修复 `Auto-Discover` 深度限制 3 导致 `AppData\Roaming\Tool\CachedData`(第4层)被跳过，提升至 5 并新增用户目录根 AI 工具隐藏目录白名单扫描；⑤ 深度 AI 扫描关键词与特征文件模式替换为真实项。**实测本机现可扫出 39 项 AI 缓存约 3.3GB**(此前全部扫不出) | WorkBuddy 调研+迭代 |
| v2.2 | 2026-08-15 | **新增"通用启发式扫描"(`Find-ElectronCaches`)，实现真正的自动添加规则**：不再依赖工具名手写清单，改为识别 Electron/Chromium 标准缓存目录结构(`CachedData`/`GPUCache`/`Code Cache`/`Dawn*Cache`/`blob_storage`/`Crashpad`/`IndexedDB` 等)；遍历 `AppData` 三目录与用户根隐藏目录，命中标准缓存名即判定为可清理缓存簇并归入所属应用。任何 Electron 套壳的新老/小众 AI 工具装了即被自动识别，无需维护清单。已集成进 [1] 深度 AI 扫描(按应用名聚焦 AI/IDE 类)与 [2] 一键扫描所有缓存。**实测本机自动识别出 298 项应用缓存约 3.4GB**，其中此前完全未知的 OpenAI Codex(`.codex`)、CherryStudio、Trae 补充会话缓存等也被自动发现 | WorkBuddy 调研+迭代 |
| v3.0 | 2026-08-15 | **跨平台 Go 重构，实现"操作系统的程序"**：① 用 Go（纯标准库、`CGO_ENABLED=0`）重写，产出 Windows/Linux/macOS(amd64+arm64) 静态二进制，摆脱 PowerShell 与 Windows 绑定；② `config` 包用 `os.User*Dir` 自动适配三系统缓存基路径（`%LocalAppData%`/`~/Library/Caches`/`~/.cache` 等）；③ 三系统已知缓存库（`db/known.go`：Windows AppData、macOS `~/Library`、Linux `~/.cache`/`~/.config`，覆盖主流 AI 工具 + 常见应用），用户根隐藏目录（`.claude`/`.workbuddy` 等）三系统复用；④ 沿用并移植 v2.2 的通用结构识别与 Electron 缓存签名（`db/electron.go`）；⑤ `.github/workflows/build.yml` 提供矩阵交叉编译 CI，打 `v*` 标签自动发 Release。本机验证：`go vet` 通过、四目标交叉编译成功、Windows 二进制实跑深度 AI 扫描正常 | WorkBuddy 重构 |
| v3.1 | 2026-08-28 | **Wails GUI 版**：`main.go` 改为 Wails 入口，`app.go` 提供前端绑定（扫描进度事件化、清理、目录管理、历史），前端 `frontend/dist`（HTML/CSS/JS 原生三件套）；`wails.json` 配置 Windows manifest 与图标 |
| v3.2 | 2026-08-28 | **新增注册表清理 + 升级缓存识别 + 数据一致性整改**：① 新增 `internal/regclean`：扫描 HKCU 下 RunMRU/RecentDocs/TypedPaths/ComDlg32 MRU/MuiCache/TrayNotify 等已知位置，**只删值不删键**且非白名单键拒绝清理，GUI 新增「注册表」Tab、CLI 新增 [5] 菜单；② **修复豆包清理误删登录信息**（数据丢失级 bug）：原规则把整个 `%LocalAppData%\Doubao`（含 `User Data` 下的 Cookies/Local State/IndexedDB 登录态）与 `%AppData%\Doubao` 配置目录列为可清理项，现改为仅清理 `Doubao/User Data/Default/{Cache,Code Cache,GPUCache,Dawn*Cache,blob_storage}` 等**纯缓存子目录**（macOS/Linux 同步去除整目录条目）；③ **补上系统软件升级缓存识别**（此前完全漏扫）：新增 `BaseProgramData`/`BaseWindows` 路径锚点，覆盖微信 4.x 升级缓存 `Tencent\xwechat\update`（**本机实测 1.3GB**）、企业微信/QQ邮箱升级缓存、`SquirrelTemp`、`C:\ProgramData\Package Cache`（**本机 191MB**）、NVIDIA 下载缓存、Windows Update 下载缓存与传递优化缓存、`C:\Windows\Temp`，并把 `ProgramData` 纳入自动发现与通用识别的扫描根；④ **修复自定义目录死功能**：`cfg.CustomDirs` 此前只保存不被扫描，新增 `scan.FindCustomCaches()` 纳入四种扫描；⑤ **修复数据竞争**：`AddCustomDir/AddExcludeDir/Remove*/Get*` 与扫描 goroutine 并发读写 `cfg` 全部加锁（`app_test.go` 并发用例覆盖）；⑥ **统一排除语义**：新增 `config.IsExcluded()`（完整路径段 + 大小写不敏感），替换原 `strings.Contains`（会误伤 `Catalogs` 等）与清理侧不一致的 basename 判定；⑦ 其他：`CleanResult` 增加 `cleaned`/结构化 `failed`（前端不再误删失败项）、清理历史改为**批量原子写**（临时文件+改名，不再每条重写全文件）、`ScanDir` 改用 `WalkDir`、清理前不再重复遍历大目录（耗时减半）、扫描防重入、前端 HTML 转义防目录名破坏 DOM、CLI `Prompt` 改为共享 stdin（不再丢多行粘贴输入）。验证：`go build/vet/test ./...` 全通过，CLI 二进制在本机实跑一键扫描验证 | WorkBuddy 扫描+整改 |
| v3.3 | 2026-08-28 | **前端界面重构为 Windows 11 Fluent 2 设计语言（仅改视觉与交互，功能零变更）**：① Mica 桌面底纹（三层柔和光晕 + SVG 噪点）+ Acrylic 半透明毛玻璃（`backdrop-filter` blur/saturate）应用于顶栏 / 侧栏 / 卡片 / 对话框 / Toast；② 布局改为 Win11 窗口式：顶栏命令条 + 左侧导航（扫描清理 / 目录规则 / 注册表 / 清理历史）+ 圆角卡片内容区 + 三项 KPI 概览（可清理项 / 预计释放 / 安全项）；③ 组件重制：6px 圆角控件、Win11 风格复选框（打勾动画）、确定态/不确定态双态进度条、胶囊徽标、圆角对话框与 Toast，统一 24×24 内联 SVG 图标精灵（16 个符号）；④ 色彩：柔和中性基底 + Win11 强调蓝（浅色 `#0078D4` / 深色 `#60CDFF`），**新增深色模式**，默认跟随系统、可手动切换并记忆到 localStorage，首绘前内联脚本防闪；⑤ 响应式三档：≥1081px 完整侧栏、≤1080px 图标栏、≤720px 底部应用栏（列表行改两行布局、触控目标加大）；⑥ 交互：Fluent 缓动曲线与错峰入场动画、Win11 焦点环、Esc / 点击遮罩关闭弹窗、焦点移入与归还、`prefers-reduced-motion` 降级；⑦ 窗口默认尺寸调整为 1140×760 以适配侧栏布局；⑧ **应用图标重绘为「扫帚 + 清洁闪光」Fluent 蓝色磁贴**：源文件 `build/windows/icon.svg`（矢量），由 `build/windows/make_icon.py`（纯标准库，PNG-in-ICO）生成 `build/windows/icon.ico`（16/24/32/48/64/128/256 七档）与 `build/appicon.png`（512）；新增 `build/windows/CacheCleaner.manifest`（由 `wails.exe.manifest` 模板解析而来，含 `com.wails.CacheCleaner` 与 DPI 感知设置）。验证：`node --check` 通过、HTML id 引用零缺失无重复、`go build/vet/test ./...` 全通过（前端由 `go:embed` 打包）；本地 `CacheCleaner.exe` 实跑验证 | WorkBuddy 界面重构 |
| v3.4 | 2026-08-28 | **清理进度可视化 + 发布 v1.0.7**：① 新增清理进度：`clean.Clean()` 增加 `ProgressFunc` 回调（`Progress{Done,Total,Freed,TotalBytes,Path,Pct}`，百分比优先按已释放字节计算），删除每一项前先回调一次，删大目录时用户能看到"当前在处理哪一项"；GUI 用**底部居中的 Acrylic 浮动任务条**展示（项数 / 百分比 / 当前路径 / 已释放大小），清理期间禁用选择与清理按钮，完成后 1.4s 自动收起；CLI 改为单行 `\r` 刷新进度；② 注册表清理同样回传首尾进度事件，表现一致；③ 新增 `App.emit()` 统一封装事件广播，`ctx` 未就绪时静默跳过（修复单测与未初始化环境下 Wails runtime 报 "invalid context"）；④ 产品版本对齐发布标签：`wails.json` productVersion 与 `CacheCleaner.manifest` 版本改为 `1.0.7` / `1.0.7.0`。验证：`go build/vet/test ./...` 全通过；`wails build -skipbindings`（CI 同款命令）本地实测通过并产出 `build/bin/CacheCleaner.exe`，PNG-in-ICO 图标能被 Wails 正常处理 | WorkBuddy 迭代 |
| v3.5 | 2026-08-29 | **社交应用缓存自动发现大升级（微信/QQ 等此前大量漏扫）**：本机取证发现 `Tencent\xwechat` 1.8G、`Tencent\WXWork` 3.2G 几乎全部漏扫，根因 6 条逐项修复：① **修错误条目**：`Tencent/WXWork/Update` 名字写错（本机实际叫 `upgrade`，本机实测 **2.0GB** 升级暂存包永远匹配不到），另补 `WXWork/patch`；② **已知库支持通配符**：`knownDef.rel` 允许 `*` 段匹配单层任意目录（`expandPath()`），覆盖 `radium/web/profiles/<随机名>/Cache`、`radium/users/<hash>/applet/codecache` 这类随机段路径；③ **新增 `config.BaseDocuments` 锚点**（含 OneDrive 重定向探测），文档目录纳入自动发现（深度 3）与通用识别（深度 3）扫描根；④ **新增 `scan/social.go` 社交容器发现**：定位 `WeChat Files` / `xwechat_files`（含 C-Z 各盘符根目录探测，覆盖用户把存储改到 D 盘）/ `Tencent Files` 三类容器，按账号应用**白名单子目录规则**——微信 3.x 只取 `FileStorage/{Cache,Thumb}` 与 `Temp`，微信 4.x 只取 `cache`/`temp`，QQ NT 只取 `nt_data/{log,Pic,Video,Emoji}` 与 `AppWebCache`；**绝不输出账号整目录**（聊天记录数据库/登录态/用户收发文件不在规则内）；⑤ **通用识别"深度加成"**：进入国产厂商总目录（Tencent/Kingsoft/Sogou/Baidu/NetEase/Alibaba/ByteDance 等 21 家）子树预算 +6、进入 Chromium 档案容器（profiles/user data/userdata）预算 +3，让 `radium/web/profiles/<随机名>/Cache`（6 层嵌套，原固定深度 3 根本够不到）这类深层标准缓存簇通用可达，且不加深非厂商子树，遍历成本可控；⑥ **自动发现关键词扩充**：新增 update/upgrade/patch/crashpad/crashinfo/caches/crash reports。新增微信 4.x 条目一批（radium 网页缓存簇、XPlugin 插件组件 637M、xlog 日志、xworker/applet 小程序缓存，**本机实测合计约 1.7GB**）与企业微信日志（160.8MB）。安全边界：`XPlugin/Plugins`、`xworker/*` 标谨慎（删除后按需重新下载，不影响聊天记录）；`Pic/Video/Emoji` 标谨慎（撤回消息除外可重新下载）；`mmkv`/`applet/data`/`applet/local`/Msg/Ptt/File_Recv/global 等会话与用户数据**一律不做条目**。验证：新增 `db/known_test.go`（通配符展开）与 `scan/social_test.go`（微信 3.x/4.x、QQ NT 沙箱结构 + 敏感目录不产出 + 厂商深度加成 + 文档目录关键词）共 7 个用例，`go vet/test ./...` 全通过；本机实跑 SmartScan（只扫描不清理）21 秒出 109 项 / 6.0GB，社交缓存(微信) 23 项 1.7GB、升级缓存 2.1GB 全部命中，敏感路径零误标 Safe | WorkBuddy 迭代 |
| v3.6 | 2026-08-29 | **系统空间回收（对齐 Dism++「空间回收」文件级项目）**：① 新增 `config.BaseSystemDrive` 锚点（由 SystemRoot 推导盘符根，如 `C:\`），支撑盘符根级条目；② 新增「系统空间回收」分类 10 条：`Windows\Logs\{CBS,DISM}`（组件服务/DISM 日志，CBS 可膨胀至 GB 级）、`Windows\{Minidump,LiveKernelReports}`（崩溃转储）、`Windows\Prefetch`（预取，谨慎——删除后短暂影响启动加速、自动重建）、`ProgramData` 与用户级 `Microsoft\Windows\WER`（错误报告）、`Local\D3DSCache`（DirectX 着色器缓存）、`$RECYCLE.BIN`（系统盘回收站，谨慎——文件不可找回，**本机实测 212.8MB**）、`Windows.old`（复核——删除后无法回退旧系统）；③ C:\Windows 下条目均需管理员权限，权限不足时列入失败项（沿用 SoftwareDistribution 既有语义）。**说明**：Dism++ 招牌的 WinSxS 组件存储清理（`Dism /Online /Cleanup-Image /StartComponentCleanup`）未纳入本轮——实测 `dism.exe` 在本机可运行但任何操作都要求提升权限（错误 740），需要 UAC 提权交互（公司机器提权可能被策略拦截），且组件清理是"执行命令"而非"删目录"，架构上属独立系统工具（类比 regclean 独立模块），作为下一步候选。验证：`go vet/test ./...` 全通过；本机实跑已知缓存扫描命中 5 项（CBS 4.7MB / DISM 263KB / WER 281KB / D3DSCache 1.2MB / 回收站 212.8MB），空目录（Minidump/Prefetch/Windows.old 等）按 size<=0 正确跳过 | WorkBuddy 迭代 |
| v3.7 | 2026-08-29 | **WinSxS 组件存储清理（Dism++ 核心能力，UAC 提权方案）**：① 新增 `internal/dismclean`：`Available()`（检测 dism.exe，本机实测可用但任何 /Online 操作都要求提升权限）、`IsElevated()`（TokenElevation 判定）、`StartAnalyze()/StartCleanup()`（提权作业，`AnalyzeComponentStore` / `StartComponentCleanup`，**不带 /ResetBase**，清理后仍可卸载已装更新）；② **提权作业模型**：非管理员进程经 `ShellExecuteW("runas")` 弹 UAC 授权启动 `cmd /c dism ... >输出文件`（已是管理员则直接隐藏执行）；输出重定向到临时文件（不写 .bat，规避中文用户名路径在 ANSI 批处理中乱码；`&&`/`||` 写 done 标志避免 %ERRORLEVEL% 延迟展开坑），`Poll()` 轮询解析 `[====20.0%====]` 进度行与 done 标志，30 分钟超时兜底，单作业互斥；③ **输出解码**：DISM 重定向输出为系统 OEM 代码页（中文 Windows 即 GBK），统一 GBK 解码（ASCII 无损）；报告解析中英文双关键词（报告大小/实际大小/可回收包数/上次清理/是否建议清理），`valueAfterColon` 按 rune 切分（全角冒号 3 字节，按字节切会乱）；④ **GUI 新增「系统工具」Tab**：组件存储状态卡（分析报告表格 / 原始输出兜底）、分析与清理按钮（UAC 前置确认弹窗）、`dism:progress` 事件驱动的进度条（未出百分比前不确定态流动条）；⑤ **CLI 新增 [6] 菜单**：分析 → 单行刷新进度 → 打印报告；⑥ 前端导航加「系统工具」图标项，侧栏版本号 v3.3→v3.4。验证：6 个解析单测（中英文报告 / 垃圾输出 / 进度行 / 全角冒号）通过，`go vet/test ./...` 全绿，`node --check` 通过，17 个新增元素 id 引用零缺失；UAC 交互流程需用户在有桌面会话的环境点击验证 | WorkBuddy 迭代 |

### AI 缓存覆盖工具一览（v2.1 扩充）

> 路径均经本机实测或官方文档（Claude / Windsurf / 通义灵码 / 文心快码 等帮助中心）核对。本机实测：迭代后单次扫描即可列出 **39 项 AI 缓存、约 3.3 GB**（此前这些国产/主流工具一个都扫不出）。

| 工具 | 典型缓存路径（相对用户目录） | 风险 | 说明 |
|------|------------------------------|------|------|
| Trae（国内版） | `AppData\Roaming\Trae CN\CachedData`、`.trae-cn` | Safe / Review | 修正了原错误路径 `AppData\Local\Trae` |
| WorkBuddy / CodeBuddy | `.workbuddy\{tmp,logs,traces,plugins,binaries,projects,sessions,tasks}`、`AppData\Local\@genieworkbuddy-desktop-updater` | Safe→Review | 本机 1.4GB+；`projects/sessions/tasks` 清理可解决"输出不准" |
| 通义灵码 / Qoder | `.lingma`、`AppData\Local\.lingma` | Review | 索引 db 可膨胀超 5GB |
| Claude | `.claude`、`AppData\Roaming\Claude` | Review / Caution | `.claude\projects` 为会话历史 |
| Windsurf / Codeium | `.codeium\windsurf`、`AppData\Roaming\Windsurf\*` | Review / Safe | `cascade` 对话历史 |
| 文心快码 Comate | `AppData\Roaming\Baidu\WenXInCode`、`.vscode\extensions\baidu-comate-*` | Caution | |
| Cline / Continue | `AppData\Roaming\Code\User\globalStorage\{saoudrizwan.claude-dev,continue.continue}` | Caution | VS Code 扩展 |
| 豆包 Doubao | `AppData\Local\Doubao\User Data` | Caution | 本机 1.8GB+ |
| Cursor / VS Code / Copilot / Ollama / LM Studio 等 | 原已知项保留 | — | 沿用 v2.0 |

### 通用启发式扫描（v2.2 引入，自动添加规则的核心）

v2.2 之后，**手写清单不再是覆盖新工具的唯一手段**。新增的 `Find-ElectronCaches` 靠缓存"结构"而非"名字"识别：

- **识别依据**：Electron / Chromium 套壳应用（几乎涵盖所有 AI 编辑器/客户端）都会产生一套标准缓存目录：`CachedData`、`GPUCache`、`Code Cache`、`DawnWebGPUCache`、`DawnGraphiteCache`、`blob_storage`、`Crashpad`、`IndexedDB`、`databases`、`Local Storage`、`Session Storage`、`Service Worker`、`workspaceStorage`、`Partitions`、`Network` 等。
- **扫描范围**：`AppData\Local` / `Roaming` / `LocalLow` 三目录（一次性浅递归 Depth 3）+ `ProgramData`（Depth 2）+ 用户目录根下所有隐藏目录（`.workbuddy` / `.trae-cn` / 未来的 `.newai` 等，Depth 5）。v3.5 起**文档目录（Documents）也纳入**（Depth 3，QQ NT/微信等社交应用的 Chromium 缓存会落在文档下），并为国产厂商总目录与 Chromium 档案容器（profiles/user data）提供**深度加成**（深层嵌套缓存簇可达）；`Downloads` / `Desktop` 等纯用户数据目录仍不递归。
- **风险分级**：纯缓存（`Cache`/`GPUCache`/`Code Cache`/`Dawn*`/`blob_storage`/`Crashpad` 等）标 `Safe`（Electron 自动重建）；含会话/配置（`IndexedDB`/`Local Storage`/`Session Storage`/`Service Worker`/`Network`/`databases` 等）标 `Caution`（清后需重登或丢失本地索引）。
- **效果**：本机实测自动识别出 **298 项应用缓存约 3.4GB**，覆盖已知与未知工具。其中此前完全未知的 OpenAI Codex(`.codex\plugins\cache`)、CherryStudio、Trae 的会话类缓存等也被自动发现。市面上任何新出的 Electron 系 AI 工具，无需改代码即可被扫出。

> 说明：v2.0 的具体迭代时间线未在仓库中留存（无 git 历史记录），上表依据脚本横幅版本号与现有代码结构推断。后续每次实质性修改请在此表追加一行，保持"版本 / 日期 / 修改内容 / 来源"四列。
