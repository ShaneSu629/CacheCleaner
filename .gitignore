# 编译产物（由 GitHub Actions 生成，勿提交）
cachecleaner
cachecleaner.exe
CacheCleaner.exe
CacheCleaner
CacheCleaner-*
cc_verify.exe
cli.exe

# 构建时生成的 .syso 资源文件（由 icon.ico + manifest 生成，可重建）
icon.syso
*.syso

# build 输出目录
build/bin/

# 用户配置与清理历史（含本机路径，勿提交）
# 注意：必须加前导 /，否则 internal/config/ 这种代码包目录也会被误忽略
/config/
CacheCleaner/config.json
CacheCleaner/history.json

# 临时文件
*.tmp
*.zip

# 本地辅助脚本 / 临时文档（个人工具，勿提交到仓库）
git_push.py
install_wails.ps1
FyneUI-Plan.md

# 项目私有数据 / 记忆目录（含本机路径与记忆，勿提交）
.workbuddy/

# 本地临时 Go 工具链（代码审查用，勿提交）
.gotoolchain/
