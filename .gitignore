# 编译产物（由 GitHub Actions 生成，勿提交）
cachecleaner
cachecleaner.exe
cachecleaner-*
cc_verify.exe

# 用户配置与清理历史（含本机路径，勿提交）
config/
CacheCleaner/config.json
CacheCleaner/history.json

# 临时文件
*.tmp

# 本地推送辅助脚本（个人工具，勿提交到仓库）
git_push.py

# 项目私有数据 / 记忆目录（含本机路径与记忆，勿提交）
.workbuddy/
