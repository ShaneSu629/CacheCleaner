# CacheCleaner 全项目扫描 · 问题梳理与整改报告

> 扫描范围：`main.go`、`app.go`、`cmd/cli/main.go`、`internal/{model,config,db,scan,ui,clean}`、
> 新增 `internal/regclean`、前端 `frontend/dist/*` 与 `frontend/wailsjs/*`、文档。
> 工具链：Go 1.25.0（本机原本未安装，临时下载便携版验证）+ `go build` / `go vet` / `go test` / `gofmt`。
> 日期：2026-08-28　版本：v3.2

---

## 一、你提出的三个需求

### 1. 新增注册表清理功能 ✅ 已完成

新增包 `internal/regclean`（Windows 实现 + 非 Windows 空实现，靠 build tag 隔离）：

| 文件 | 作用 |
|------|------|
| `regclean.go` | 类型定义（`Entry`）、包说明 |
| `regclean_windows.go` | 白名单表（8 个位置）+ `Scan()` + `Clean()` |
| `regclean_other.go` | 非 Windows 平台返回空 / 提示不支持 |

覆盖位置（全部 `HKCU`，无需管理员权限）：

| 注册表位置 | 风险 | 说明 |
|------------|------|------|
| `…\Explorer\RunMRU` | 安全 | 运行对话框输入历史 |
| `…\Explorer\RecentDocs` | 安全 | 最近打开文档历史 |
| `…\Explorer\TypedPaths` | 安全 | 地址栏输入历史 |
| `…\Explorer\ComDlg32\OpenSavePidlMRU` | 安全 | 打开/保存对话框文件名历史 |
| `…\Explorer\ComDlg32\LastVisitedPidlMRU` | 安全 | 打开/保存对话框位置历史 |
| `…\Explorer\Map Network Drive MRU` | 安全 | 映射网络驱动器历史 |
| `…\Windows\Shell\MuiCache` | 安全 | 程序显示名称缓存 |
| `…\CurrentVersion\TrayNotify` | 谨慎 | 托盘图标通知历史（清理后隐藏图标设置重置） |

**安全设计**：① 只做「已知位置」扫描，绝不做开放式注册表搜索；② `Clean()` 对传入键做
**白名单校验**，非白名单键（含 `HKLM\…\Run` 等）一律拒绝；③ **只删值、不删键**，Windows 会自动重建；
④ GUI 与 CLI 均需二次确认。

**接入**：GUI 新增「注册表」Tab（`ScanRegistry` / `CleanRegistry` 绑定），CLI 新增菜单 `[5]`，
清理结果统一写入清理历史。

**本机实跑验证**：扫出 **5 项 / 43.7 KB / 383 个值**（RunMRU 6、RecentDocs 150、TypedPaths 6、
LastVisitedPidlMRU 24、MuiCache 197）；未出现的 3 个位置是本机尚未触发的 MRU（键不存在即跳过）。
清理路径通过「自建临时键 → 清理 → 断言清空 → 删除」的单测验证，未动用户真实数据。

### 2. 系统软件升级缓存漏扫，清理不彻底 ✅ 已完成

根因：扫描根只有 `%LocalAppData%` / `%AppData%` / `LocalLow` / 用户根隐藏目录，
**`ProgramData` 与 `C:\Windows` 完全不在视野内**，且升级缓存目录名（`xwechat\update`、
`Package Cache`、`SquirrelTemp`）不匹配关键词规则，因此一个都扫不到。

整改：

- `config` 新增 `BaseProgramData`、`BaseWindows` 两个路径锚点（`ProgramData` / `SystemRoot` 环境变量）。
- `db/known.go` 新增升级缓存条目：微信 4.x `Tencent\xwechat\update`、企业微信 `WXWork\Update`、
  QQ 邮箱 `WeMail\downloading`、微信 3.x `WeChat\XPlugin`、`SquirrelTemp`、
  `ProgramData\Package Cache`（复核级，删除影响卸载/修复）、NVIDIA `Downloader`（安全）与
  `Installer2`（复核）、`Windows\SoftwareDistribution\Download`（复核，需管理员）、
  传递优化缓存（复核）、`Windows\Temp`（谨慎）。
- `ProgramData` 纳入自动发现（深度 2）与通用 Electron 识别（深度 2）的扫描根。

**本机实测（整改后一键扫描）**：

| 项 | 大小 | 风险 | 整改前 |
|----|------|------|--------|
| `Tencent\xwechat\update` 微信 4.x 升级缓存 | **1.3 GB** | 安全 | ❌ 完全漏扫 |
| `C:\ProgramData\Package Cache` | **189.9 MB** | 复核 | ❌ 完全漏扫 |
| `Doubao\User Data\update_downloads` | — | 安全 | ❌ 漏扫 |
| `Windows\SoftwareDistribution\Download` | 0 B | 复核 | ❌ 漏扫 |
| `WXWork\Update`、`WeMail\downloading` | 0 B | 安全/谨慎 | ❌ 漏扫 |

### 3. 清理豆包会连登录信息、配置一起删掉 ✅ 已修复（数据丢失级 bug）

根因（`db/known.go` 原内容）：

```go
{"windows", config.BaseLocal,   "Doubao",            "AI缓存", model.RiskSafe,    "豆包缓存"},     // ← 整个 Doubao 目录
{"windows", config.BaseLocal,   "Doubao/User Data",  "AI缓存", model.RiskCaution, "豆包用户数据"},  // ← 整个用户数据目录
{"windows", config.BaseRoaming, "Doubao",            "AI缓存", model.RiskCaution, "豆包配置"},      // ← 整个配置目录
```

本机取证确认 `%LocalAppData%\Doubao\User Data\` 是完整 Chromium 用户数据目录，内含
`Local State`、`Cookies`、`Login Data`、`IndexedDB`、`Bookmarks`、`History` 等登录态与配置；
`%AppData%\Doubao\` 下是 `public_config.json`。而第一条被标为 **RiskSafe**，
点「仅清理安全项」就会把登录态整目录删掉；再加上 `dedupeParents` 会保留祖先、
丢弃子孙，导致子目录条目被吞掉，用户根本看不出自己删的是什么。

整改：删除三个整目录条目，改为**仅清理纯缓存子目录**（macOS/Linux 同步去除整目录条目）：

```
Doubao\User Data\Default\{Cache, Code Cache, GPUCache, DawnGraphiteCache, DawnWebGPUCache, blob_storage}
Doubao\User Data\{GrShaderCache, GraphiteDawnCache, ShaderCache, Crashpad,
                  component_crx_cache, extensions_crx_cache}
Doubao\User Data\update_downloads（升级下载缓存）/ update_data（谨慎）
```

**本机实跑验证**：整改后豆包只列出 `Default\Cache` 271.8 MB、`Code Cache` 37.6 MB、
`GrShaderCache` 5.3 MB 等纯缓存子目录，**`Doubao` 与 `Doubao\User Data` 整目录不再出现在结果中**。

---

## 二、扫描发现的其他问题（按严重度）

### P0 — 已修复

| # | 问题 | 影响 | 整改 |
|---|------|------|------|
| 1 | **数据竞争**：`AddCustomDir`/`AddExcludeDir`/`Remove*`/`Get*` 完全不加锁，而扫描 goroutine 正在并发读 `cfg`（Wails 每个绑定调用独立 goroutine） | 并发写 map/slice 可能 panic 或配置错乱 | 全部 `cfg` 读写纳入 `mu`；`Scan` 启动前快照 `cfg` 指针；新增 `app_test.go` 并发用例 |
| 2 | **自定义目录是死功能**：`cfg.CustomDirs` 只在配置读写与 UI 增删中出现，`scan` 包从未读取 | 用户添加的自定义目录永远扫不出来 | 新增 `scan.FindCustomCaches()`，纳入一键/AI 扫描（第 4 阶段） |
| 3 | **配置加载失败后 `cfg` 为 nil**，后续所有绑定方法空指针 panic | 启动异常后界面点击即崩溃 | `startup` 失败时回退 `config.Default()`，保证 `cfg` 非 nil |

### P1 — 已修复

| # | 问题 | 影响 | 整改 |
|---|------|------|------|
| 4 | **排除语义不一致 + 子串误伤**：扫描侧 `strings.Contains(full, ex)`，清理侧只比 basename；且 `Contains` 会让排除 `logs` 误伤 `Catalogs`、排除 `temp` 误伤 `Temporary` | 扫描与清理结果对不上；静默漏清/漏扫 | 新增 `config.IsExcluded()` 作为唯一入口：完整路径段 + 大小写不敏感匹配，两侧统一（`config_test.go` 13 个用例） |
| 5 | **扫描可重入**：连点扫描按钮会起两个 goroutine，进度与结果事件交错 | 界面进度乱跳、结果被覆盖 | 后端加 `scanning` 保护并发 `scan:busy` 事件；前端扫描期间禁用按钮 |
| 6 | **失败项被前端误移除**：清理后前端按「选中的全部路径」过滤，失败项也一起消失 | 失败项静默丢失，无法重试 | `CleanResult` 新增 `cleaned []string` 与结构化 `failed [{path,error}]`，前端只移除 `cleaned` 中的项 |
| 7 | **CLI `Prompt` 每次新建 `bufio.Reader`** | 一次粘贴多行输入时，后续行被丢弃 | 改为包级共享 stdin |

### P2 — 已修复

| # | 问题 | 整改 |
|---|------|------|
| 8 | 清理历史逐条全量读写 `history.json`（N 项 = N 次全文件 IO），且非原子写（并发刷新历史页可能读到半个文件） | 改为**批量一次性写入 + 临时文件改名（原子替换）**，新增 `AppendHistory` 供注册表清理复用 |
| 9 | 清理前对每个目录再遍历一遍统计大小 | 复用扫描时已统计的 `e.Size`，大目录清理耗时约减半 |
| 10 | `ScanDir` 用 `filepath.Walk`（每条多一次 `Lstat`） | 改用 `filepath.WalkDir` |
| 11 | 前端大量 `innerHTML` 拼接未转义（目录名可含引号/尖括号，会破坏 DOM 与 title 属性） | 新增 `esc()` 并覆盖列表、图表、目录清单、历史表格 |
| 12 | 文档与现状不符（仍写「纯标准库无外部依赖」「入口 CLI」，无 Wails/注册表/升级缓存内容） | 更新 `技术文档.md`（概述表、架构图、关键技术点、CHANGELOG 追加 v3.1/v3.2）与 `README.md`（功能特性、菜单） |

---

## 三、验证结果

```
gofmt -s -l  ：代码格式无问题（列出的差异仅因仓库文件为 CRLF 行尾，未做全量转换）
go build ./...  ✅ 通过
go vet   ./...  ✅ 通过
go test  ./...  ✅ 全绿
   cachecleaner              并发配置读写、失败项保留
   internal/config           排除匹配 13 用例
   internal/scan             去重、已知项优先、自定义目录扫描
   internal/clean            清理 + 历史批量原子写、排除跳过
   internal/regclean         白名单定义、扫描白名单、拒绝非白名单键、真实删除（临时键沙箱）
```

实跑验证（CLI 二进制在本机真实执行）：一键扫描 29 秒完成，升级缓存与豆包缓存子目录均已列出，
豆包整目录已消失；注册表扫描 5 项 43.7 KB。

---

## 四、遗留事项与建议

1. **同类整目录风险（建议下一步处理）**：`db/known.go` 中仍有若干「整个应用数据目录」条目，
   例如 `BaseLocal\Windsurf`、`BaseLocal\WorkBuddy`、`BaseRoaming\DeepSeek`、
   `BaseRoaming\Claude\IndexedDB`。它们与豆包是同一类风险（会删掉登录态/配置），
   只是本次没有本机取证依据，未一并修改。建议逐个核对目录结构后改成纯缓存子目录。
2. **`-race` 未在本机执行**：本机无 C 编译器（无 gcc），`go test -race` 需要 cgo。
   建议在 CI（Linux runner 自带 gcc）中增加 `go test -race ./...` 步骤。
3. **行尾**：仓库 Go 文件为 CRLF，新文件为 LF，未做全量统一（避免巨型 diff）。
   如需统一，建议同时加 `.gitattributes` 明确行尾策略。
4. **需管理员权限的项**（`Windows\Temp`、`SoftwareDistribution\Download`、传递优化缓存）
   在无权限环境下删除会失败，会正常出现在「失败 N 项」中，不会静默吞掉。
5. `golang.org/x/sys` 已由间接依赖转为直接依赖（注册表清理使用），`go.mod` 已整理。

---

## 五、变更文件清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `internal/regclean/regclean.go` | 新增 | 注册表垃圾项类型 |
| `internal/regclean/regclean_windows.go` | 新增 | Windows 扫描/清理实现 |
| `internal/regclean/regclean_other.go` | 新增 | 非 Windows 空实现 |
| `internal/regclean/regclean_windows_test.go` | 新增 | 白名单、拒绝越权、沙箱删除验证 |
| `internal/config/config.go` | 修改 | 新增 ProgramData/Windows 锚点、`Default()`、`IsExcluded()` |
| `internal/config/config_test.go` | 新增 | 排除匹配语义用例 |
| `internal/db/known.go` | 修改 | 豆包精确化、升级缓存条目 |
| `internal/db/util.go` | 修改 | `Walk` → `WalkDir` |
| `internal/scan/scanner.go` | 修改 | 统一排除、ProgramData 扫描根、自定义目录扫描、阶段 4 |
| `internal/scan/scanner_test.go` | 新增 | 去重、已知项优先、自定义目录 |
| `internal/clean/cleaner.go` | 修改 | 排除统一、历史批量原子写、`AppendHistory`、返回已清理项 |
| `internal/clean/cleaner_test.go` | 新增 | 清理、历史批量写、排除跳过 |
| `internal/ui/menu.go` | 修改 | 共享 stdin、菜单 `[5]`、`SelectRegistry` |
| `app.go` | 修改 | 并发安全、nil 兜底、防重入、CleanResult 结构化、注册表绑定 |
| `app_test.go` | 新增 | 并发读写与失败项保留用例 |
| `cmd/cli/main.go` | 修改 | 新增 `[5] 注册表垃圾清理`、适配新签名 |
| `frontend/dist/index.html` | 修改 | 新增注册表 Tab 与面板 |
| `frontend/dist/app.js` | 修改 | 注册表逻辑、HTML 转义、失败项保留、扫描期间禁用按钮 |
| `frontend/dist/style.css` | 修改 | 注册表面板与按钮禁用态样式 |
| `frontend/wailsjs/go/main/App.js`、`App.d.ts`、`go/models.ts` | 修改 | 同步新增绑定与 DTO 类型 |
| `go.mod` | 修改 | `golang.org/x/sys` 转为直接依赖 |
| `.gitignore` | 修改 | 忽略临时工具链目录 |
| `技术文档.md`、`README.md` | 修改 | 概述/架构/功能/菜单/CHANGELOG 同步 |
